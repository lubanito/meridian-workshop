# Frontend dev server config. Copy to `.env.local` and edit as needed.
# Vite only exposes vars prefixed with `VITE_` to client code.

# Backend API origin + path. Defaults to the local FastAPI server.
VITE_API_BASE_URL=http://localhost:8001/api
